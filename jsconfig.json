{
  "compilerOptions": {
    // This must be specified if "paths" is set
    "baseUrl": ".",
    // Relative to "baseUrl"
    "paths": {
      "ui/*": ["src/*"]
    }
  },
  "publishConfig": { "registry": "https://npm.pkg.github.com/" }
}
